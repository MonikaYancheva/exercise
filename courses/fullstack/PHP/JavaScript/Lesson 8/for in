var person,
    property;

    person = {
    	firstName: "John",
    	lastName:"Doe",
    	age: 50,
    	eyeColor: "blue",
    	hairColor: "black",
    	nationality: "bulgarian"

    }

    for(property in person) {
    	if(lastName == "Doe" && eyeColor == "blue"){
    		console.log(person.lastName + ": " + peson.lastName[property] + "<br>" + person.eyeColor + ": " + person.eyeColor[property]);
    	}
    	else {
    		cosole.log("It's undefined variable");
    	}
    }
